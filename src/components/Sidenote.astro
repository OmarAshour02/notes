---
/**
 * Tufte-style margin note. Floats into the right gutter on desktop;
 * collapses to an inline callout on narrow screens. Auto-numbered via
 * a CSS counter on body.
 */
---

<aside class="sidenote">
  <slot />
</aside>
