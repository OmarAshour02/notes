---
type Props = { tags: string[] };
const { tags } = Astro.props;
---

<p class="filed-under">
  <span class="label">Filed under</span>
  {tags.map((t) => (
    <>
      <span class="sep">·</span>
      <span class="tag">{t}</span>
    </>
  ))}
</p>
